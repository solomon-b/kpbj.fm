# Weeder Configuration
# https://github.com/ocharles/weeder

# Root modules that should be considered entry points
roots = [
  '''^Main\.main$''',
  '''^Paths_.*$''',
]

root-modules = [
  # TODO: This root shouldn't be needed but the `roots` section isn't working:
  '''^API$''',
  '''^Component.Form.JSArrow''',
  '''^Lucid\.Extras$''',
  '''^Utils$''',
]

type-class-roots = true
