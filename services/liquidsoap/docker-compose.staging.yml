# Staging VPS overrides
# Usage: docker compose -f docker-compose.yml -f docker-compose.staging.yml up -d

services:
  icecast:
    ports:
      - "8001:8000"

  liquidsoap:
    environment:
      - API_BASE=https://staging.kpbj.fm/api/playout
      - PLAYOUT_SECRET=${PLAYOUT_SECRET:-}
